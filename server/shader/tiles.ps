#version 120

uniform sampler2DRect tex;
uniform vec4 color;
varying vec2 tex_coords;

void main(){
	gl_FragColor = texture2DRect(tex, tex_coords);
	//gl_FragColor = 0.5 * texture2DRect(tex, tex_coords) + 0.5 * color;
}